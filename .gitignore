# macOS
.DS_Store

# Xcode: local build output and per-user window state
build/
DerivedData/
*.xcuserstate
**/xcuserdata/

# Signing material. A private key must never reach the repo.
*.p12
*.p8
*.cer
*.mobileprovision
*.provisionprofile

# Agent guides live on disk only: Claude Code reads CLAUDE.md from the
# working tree, and keeping them out of git keeps the repo front page clean.
/AGENTS.md
/CLAUDE.md
